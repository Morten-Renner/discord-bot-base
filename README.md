# discord-bot-base

## production setup

// TODO: production setup

## development setup

Clone the GitHub repository and run
```npm install --production=false```
in order to install all dependencies.

Compile TypeScript automatically on file saves with the tsc watch mode:
```npm run watch```

Start the bot with nodemon:
```npm run dev```
